export const selectQuery = ({ search }) => search?.query;
export const selectResults = ({ search }) => search?.results;
export const selectCurrentState = ({ search }) => search?.currentState;
export const selectCurrentPage = ({ search }) => search?.currentPage;
export const selectTotalPages = ({ search }) => search?.totalPages;
